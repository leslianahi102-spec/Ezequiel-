document.addEventListener('DOMContentLoaded', () => {
    // Inicializar animaciones AOS
    AOS.init({
        duration: 1000,
        once: false,
        mirror: true,
        offset: 80
    });

    console.log("Blog Personal de Ramon R. cargado correctamente.");
});